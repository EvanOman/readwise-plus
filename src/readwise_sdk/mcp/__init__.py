"""Readwise MCP server — an optional interface exposed via ``readwise-plus[mcp]``.

Install with the ``mcp`` extra (``pip install readwise-plus[mcp]``) and run the
``readwise-mcp`` console script, or ``python -m readwise_sdk.mcp``.
"""
